# 聊天交互迁移待修改计划

参考项目：`D:\Desktop\开发工具\claudeGui\claudinal`

目标：当前 `packages/gui` 的聊天主界面、输入框、消息流、运行过程、附件与快捷交互，整体按参考项目的交互方式迁移，但底层适配 OpenCode 的 session、message、part、permission、model、command API。

## 一、参考项目聊天交互功能清单

### 1. 聊天布局

- 空会话展示欢迎态，输入框居中。
- 有消息后展示紧凑头部、消息流、底部输入框。
- 消息列表最大宽度约 `max-w-3xl`，居中显示。
- 输入框固定在底部，使用背景承接，不展示内部运行状态栏。
- 顶部只展示会话标题、项目名和更多操作，不展示连接状态、线程数量、执行阶段条。

### 2. 输入框 Composer

- 文本框自适应高度，最高约 `240px`。
- `Enter` 发送，`Shift + Enter` 换行，中文输入法组合中不误发送。
- 运行中按 `Esc` 中断当前回合。
- 运行中输入框 placeholder 变为“要求后续变更”，可继续提交后续消息。
- 运行中保留独立停止按钮，同时发送按钮变为“排队/追加”语义。
- 输入框左侧 `+` 菜单包含：
  - 添加照片和文件。
  - 计划模式开关。
  - 插件入口或插件列表。
  - 协同模式入口，当前项目无对应 OpenCode 能力，先不启用。
- 输入框底部展示：
  - 权限模式选择。
  - Git 分支选择，当前项目需补本地 Git 状态/分支能力后再完整接入。
  - 模型选择。
  - 计划用量，当前项目无 Anthropic OAuth 用量数据，先不接。
- 支持外部文本注入到输入框，并自动聚焦；当前项目后续可接建议卡片。

### 3. 附件与图片

- 支持点击添加文件。
- 支持粘贴图片/文件。
- 支持拖拽文件到输入框区域。
- 图片以缩略图 chip 展示，可点击预览大图。
- 文本文件以 chip 展示，参考项目会把文本内容包进 `<uploaded_file>` 后并入 prompt；当前项目已使用 OpenCode file part，优先保留 file part 发送。
- 附件显示名称、大小、类型图标和移除按钮。
- 对超限或不支持的文件给出可见提示。

### 4. `@` 与 `/` 补全

- 输入 `@` 后根据当前项目文件列表补全路径。
- 输入 `/` 后根据命令列表补全 slash command。
- 补全面板支持：
  - 上下键切换。
  - `Tab` 或 `Enter` 应用选中项。
  - `Esc` 关闭。
  - 鼠标悬停和点击选择。
  - 无匹配项提示。
- 当前项目需要新增 OpenCode 文件搜索和命令列表桥接。

### 5. 消息流 MessageStream

- 空消息显示“就绪 / 输入消息开始对话”。
- 用户消息靠右气泡展示，支持复制。
- 助手消息靠左展示，支持复制。
- 助手 markdown 富文本渲染参考项目较完整；当前项目先迁移排版、复制、换行与代码块基础样式。
- 直播中无内容助手消息显示“思考中/正在生成”。
- 消息列表自动滚动到底部。

### 6. 运行过程 RunGroup

- 用户消息后创建一组“处理中…”运行过程。
- `thinking`、`tool_use`、`tool_result`、文件变更等过程聚合到可折叠的运行组。
- 运行中展开，完成后自动收起。
- 展示运行时长、步骤数量和 loading 状态。
- 工具行使用图标和自然语言标签，不展示内部 event type/session id。

### 7. 工具、文件和 diff 展示

- 工具调用可折叠。
- Bash/PowerShell 展示 terminal 样式。
- Read/Write/Edit/MultiEdit/Grep/Glob 根据工具类型展示文件、命令、diff 或参数。
- Tool result 支持折叠预览，长输出可展开全部。
- 文件 diff 使用新增/删除颜色。
- 当前 OpenCode message part 字段不同，需要适配 `kind/tool/file/title/status/text/raw`。

### 8. ChatHeader

- 只展示标题、项目名和更多菜单。
- 更多菜单包含打开项目目录、复制会话 ID、删除/归档/重命名等。
- 当前项目先接入打开项目目录、复制会话 ID、删除聊天；归档/重命名后续补 session API 后再接。
- diff 按钮可保留为右侧详情面板入口或后续独立 diff 入口。

### 9. 发送、停止与队列语义

- 非运行中：发送按钮为上箭头。
- 运行中：显示停止按钮，发送按钮变为追加/排队图标。
- 当前 OpenCode 使用 `prompt_async`，需要允许运行中继续发送；若 OpenCode 返回 busy 或失败，保留错误提示。
- 发送后立即显示本地用户消息/或尽快通过刷新展示，避免“没反应”。

## 二、当前项目需要修改的文件

- `packages/gui/src/features/thread/thread-workspace.tsx`
  - 迁移 Composer、SuggestionPanel、MessageStream、RunGroup、MessageBlock、Header 交互。
- `packages/gui/src/app/app.tsx`
  - 传入 slash commands、删除聊天、打开项目目录、允许运行中追加发送。
- `packages/gui/src/lib/tauri.ts`
  - 新增文件搜索、命令列表类型和调用。
- `packages/gui/src-tauri/src/opencode/mod.rs`
  - 新增 OpenCode `/find/file` 和 `/command` 请求封装。
- `packages/gui/src-tauri/src/commands/mod.rs`
  - 新增 Tauri command 输入类型和桥接函数。
- `packages/gui/src-tauri/src/main.rs`
  - 注册新增 command。

## 三、实施顺序

1. 输入框交互对齐：自适应高度、拖拽、粘贴、附件 chip、图片预览、运行中可追加发送、独立停止按钮。
2. 补全能力接入：新增文件搜索和命令列表 bridge，实现 `@`/`/` 面板。
3. 消息流对齐：用户气泡、助手正文、复制、自动滚动、空态、运行中思考态。
4. 运行过程对齐：把 OpenCode parts 和 live activities 聚合为可折叠 RunGroup，隐藏内部 event/session id。
5. 头部对齐：只保留标题、项目名、更多菜单，接入打开目录、复制 ID、删除聊天。
6. 验证：运行 `npx --yes bun@1.3.13 --cwd packages/gui typecheck` 和 `cargo check`。

## 四、暂缓项

- 协同模式：参考项目是 Claude GUI 自有协同流程，当前 OpenCode GUI 暂无等价后端。
- Git 分支切换：参考项目有独立 Git IPC，当前项目后续可补本地 Git 命令桥接。
- Plan 用量：参考项目依赖 Anthropic OAuth usage，当前 OpenCode 多 provider 场景暂无统一数据源。
- 完整 Markdown/语法高亮：当前先实现基础 markdown/代码块排版，后续再引入专用渲染组件。
